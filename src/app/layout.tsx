import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "@/components/providers";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Ravindu Nilash - Software Engineer",
  description: "Portfolio of Ravindu Nilash, a passionate software engineer specializing in modern web development.",
  keywords: ["Ravindu Nilash", "Software Engineer", "Web Developer", "React", "Next.js", "TypeScript"],
  authors: [{ name: "Ravindu Nilash" }],
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.variable} font-sans antialiased`}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
